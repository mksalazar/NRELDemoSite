<!DOCTYPE html>
<html lang="en">
<link(rel='stylesheet', href='/css/bootstrap.min.css')>
<link href='/css/styles.css' rel="stylesheet">
<link href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" rel="stylesheet" >
<head>
  <meta charset="UTF-8">
  <title>MY APP</title>
</head>
<body>
  <script src="/main.js"></script>
  <h2 class="title"> NREL CRUD DB DEMO SITE </h2>
  <div class="container">
    <div class="row row-cols-auto">
        <div class="col">
            <p class="sub-titles">Example of CREATE an elemnt in DB</p>
            <form action="/input" method="POST">
                <input type="text" placeholder="input" name="input">
                <button type="submit">Submit</button>
            </form>
        </div>
        <div class="col">
        <p class="sub-titles">List of Current Elements in DB (which are fetched and rendered via READ from DB</p>
            <ul class="list">
                <% for(var i=0; i < listItems.length; i++) {%>
                    <li class="list">
                        <div><%= listItems[i].input %></div>
                    </li>
                <% } %>
            </ul>
        </div>
        <div class="col">
            <p class="sub-titles">UPDATE an existing element in the DB</p>
            <button id="update-button">Replace existing element in the DB</button>
        </div>
        <div class="col">
            <p class="sub-titles">DELETE an existing element in the DB</p>
            <button id="delete-button">Delete Darth Vadar's quote</button>
        </div>
    </div>
</div>
</body>
</html>